<%= content_for :title, @document.name || 'New document' %>

<section class="document-menu">

    <h3>codefork</h3>

    <ul class="account">
        <% if user_signed_in? %>
            <li><%= current_user.username %></li>
            <li><%= link_to 'Logout', logout_path %></li>
        <% else %>
            <li><%= link_to 'Sign in with Github', '/auth/github' %></li>
        <% end %>
    </ul>

    <%= yield :menu %>

</section>